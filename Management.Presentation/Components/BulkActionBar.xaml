<UserControl x:Class="Management.Presentation.Components.BulkActionBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="80" d:DesignWidth="1200">
    <Border Background="{DynamicResource AccentBrush}" Height="80" CornerRadius="0">
        <Border.RenderTransform>
            <TranslateTransform Y="-80"/>
        </Border.RenderTransform>
        <Border.Triggers>
            <EventTrigger RoutedEvent="Loaded">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                                         To="0" Duration="0:0:0.3">
                            <DoubleAnimation.EasingFunction>
                                <CubicEase EasingMode="EaseOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Border.Triggers>
        
        <Grid Margin="48,0">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Button Command="{Binding CloseSelectionCommand}" Style="{StaticResource IconButtonStyle}" Margin="0,0,24,0">
                    <TextBlock Text="✕" Foreground="White" FontSize="18"/>
                </Button>
                <TextBlock Text="{Binding SelectionCountText}" Foreground="White" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button Content="{DynamicResource TerminologyExportCSVLabel}" 
                        Command="{Binding ExportCommand}"
                        Style="{StaticResource PrimaryButton}" 
                        Background="{DynamicResource WindowBg}" 
                        Foreground="{DynamicResource TextPrimaryBrush}" 
                        Margin="0,0,12,0" Padding="20,0"/>
                <Button Content="{DynamicResource TerminologyBulkDeleteLabel}" 
                        Command="{Binding DeleteCommand}"
                        Style="{StaticResource PrimaryButton}" 
                        Background="{DynamicResource SurfaceBg}" 
                        Foreground="{DynamicResource TextOnDarkBrush}" 
                        Opacity="0.8" Padding="20,0"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
