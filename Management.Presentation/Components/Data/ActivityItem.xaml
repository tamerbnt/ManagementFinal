<UserControl x:Class="Management.Presentation.Components.Data.ActivityItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="400">
    <Grid Height="60">
        <Border BorderBrush="{DynamicResource Brush.Border.Subtle}" BorderThickness="0,0,0,1">
            <Grid Margin="12,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="40" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- Avatar/Icon -->
                <Ellipse Width="32" Height="32" Fill="{DynamicResource Brush.Brand.Primary}" Opacity="0.25" VerticalAlignment="Center" />
                <Path Data="{Binding Icon}" 
                      Fill="{DynamicResource Brush.Brand.Primary}" 
                      Stretch="Uniform" 
                      Width="18" Height="18" 
                      HorizontalAlignment="Center" VerticalAlignment="Center" />

                <!-- Text Info -->
                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="12,0">
                    <TextBlock Text="{Binding Title}" FontWeight="SemiBold" Foreground="{DynamicResource Brush.Text.Primary}" FontSize="13" />
                    <TextBlock Text="{Binding Subtitle}" Foreground="{DynamicResource Brush.Text.Secondary}" FontSize="12" TextTrimming="CharacterEllipsis" />
                </StackPanel>

                <!-- Timestamp -->
                <TextBlock Grid.Column="2" 
                           Text="{Binding Timestamp}" 
                           Foreground="{DynamicResource Brush.Text.Secondary}" 
                           FontSize="11" 
                           VerticalAlignment="Center" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
