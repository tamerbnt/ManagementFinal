<UserControl x:Class="Management.Presentation.Views.Restaurant.KitchenDisplayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200">
    <Grid Background="{DynamicResource WindowBg}">
        <Border Padding="32">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="{DynamicResource TerminologyKitchenPrepLabel}" Style="{StaticResource SectionHeader}" Margin="0,0,0,32"/>

                <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding ActiveKitchenOrders}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Width="300" Background="{DynamicResource SurfaceBg}" CornerRadius="12" Margin="0,0,24,0" VerticalAlignment="Top">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <!-- Header -->
                                        <Border Background="{DynamicResource WindowBg}" CornerRadius="12,12,0,0" Padding="16">
                                            <Grid>
                                                <TextBlock Text="{Binding TableNumber}" Foreground="{DynamicResource TextPrimaryBrush}" FontWeight="Bold" FontSize="18"/>
                                                <TextBlock Text="{Binding ElapsedTime}" 
                                                           Foreground="{Binding ElapsedTimeSpan, Converter={StaticResource TimeSpanToOverdueBrush}}" 
                                                           FontWeight="Bold" FontSize="18"
                                                           HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>

                                        <!-- Items -->
                                        <Border Grid.Row="1" Padding="16">
                                            <ItemsControl ItemsSource="{Binding Items}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Grid Margin="0,0,0,8">
                                                            <TextBlock Text="{Binding Quantity, StringFormat={}{0}x}" Foreground="{DynamicResource FacilityAccentBrush}" FontWeight="Bold"/>
                                                            <TextBlock Text="{Binding Name}" Foreground="{DynamicResource TextPrimaryBrush}" Margin="32,0,0,0" FontSize="16"/>
                                                        </Grid>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </Border>

                                        <!-- Action -->
                                        <Button Grid.Row="2" Style="{StaticResource PrimaryButton}" 
                                                Height="96" Margin="16" FontSize="20" FontWeight="Bold"
                                                Command="{Binding DataContext.MarkReadyCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}">
                                            <StackPanel Orientation="Horizontal">
                                                <Path Data="M22 11.08V12a10 10 0 1 1-5.93-9.14" Stroke="{DynamicResource TextOnAccentBrush}" StrokeThickness="3" Margin="0,0,16,0" VerticalAlignment="Center"/>
                                                <Path Data="M22 4L12 14.01l-3-3" Stroke="{DynamicResource TextOnAccentBrush}" StrokeThickness="3" VerticalAlignment="Center"/>
                                                <TextBlock Text="{DynamicResource TerminologyMarkReadyLabel}" Foreground="{DynamicResource TextOnAccentBrush}" Margin="16,0,0,0" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Button>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>
