<UserControl x:Class="Management.Presentation.Views.Onboarding.Steps.FinishView"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
              xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
              xmlns:converters="clr-namespace:Management.Presentation.Converters"
              mc:Ignorable="d" 
              d:DesignHeight="450" d:DesignWidth="400">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter" />
        <converters:InverseBooleanConverter x:Key="InverseBoolConverter" />
    </UserControl.Resources>

    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <!-- Animated Icon -->
            <Grid Width="140" Height="140" Margin="0,0,0,40">
                <Path x:Name="SuccessIcon" 
                      Data="{StaticResource Icon.CheckCircle}" 
                      Fill="{DynamicResource Brush.Success}" 
                      Stretch="Uniform"
                      RenderTransformOrigin="0.5,0.5">
                    <Path.RenderTransform>
                        <ScaleTransform ScaleX="1" ScaleY="1" />
                    </Path.RenderTransform>
                </Path>
            </Grid>

            <StackPanel HorizontalAlignment="Center" Margin="0,0,0,32">
                <TextBlock Text="{DynamicResource Strings.Onboarding\Steps.YoureAllSet}" 
                           FontSize="32" FontWeight="Bold" 
                           Foreground="{DynamicResource Brush.Text.Primary}"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="{DynamicResource Strings.Onboarding\Steps.Yourapplicationisnowconfi}" 
                           FontSize="18" Foreground="{DynamicResource Brush.Text.Secondary}"
                           Margin="0,12,0,0" TextWrapping="Wrap" TextAlignment="Center"/>
            </StackPanel>

            <!-- Error Message -->
            <Border Background="{DynamicResource Brush.Danger.Dim}" 
                    CornerRadius="{DynamicResource Corner.MD}" 
                    Padding="12"
                    Margin="0,0,0,16"
                    Visibility="{Binding HasError, Converter={StaticResource BoolToVisConverter}}">
                <TextBlock Text="{Binding ErrorMessage}" 
                           Foreground="{DynamicResource Brush.Danger}" 
                           FontSize="14"
                           TextWrapping="Wrap"/>
            </Border>

            <!-- Status Message -->
            <TextBlock Text="{Binding StatusMessage}" 
                       Foreground="{DynamicResource Brush.Text.Secondary}"
                       Margin="0,0,0,16"
                       Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisConverter}}"
                       HorizontalAlignment="Center"/>

            <Button Content="{DynamicResource Strings.Onboarding\Steps.GotoDashboard}" 
                    Command="{Binding FinishCommand}"
                    IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBoolConverter}}"
                    Style="{StaticResource Button.Primary}"
                    Width="280" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>

    <UserControl.Triggers>
        <EventTrigger RoutedEvent="UserControl.Loaded">
            <BeginStoryboard>
                <Storyboard RepeatBehavior="Forever">
                    <DoubleAnimation Storyboard.TargetName="SuccessIcon"
                                     Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                     From="1.0" To="1.1" Duration="0:0:1.5" AutoReverse="True">
                        <DoubleAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseInOut" />
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                    <DoubleAnimation Storyboard.TargetName="SuccessIcon"
                                     Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                     From="1.0" To="1.1" Duration="0:0:1.5" AutoReverse="True">
                        <DoubleAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseInOut" />
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </UserControl.Triggers>
</UserControl>
