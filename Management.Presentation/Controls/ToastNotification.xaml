<UserControl x:Class="Management.Presentation.Controls.ToastNotification"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="80" d:DesignWidth="420"
             Width="420" MinHeight="80">

    <!--
        NOTE: BoolToVisibilityConverter is registered globally via
        Resources/Converters.xaml (merged in App.xaml).
        No local redeclaration needed.
    -->

    <UserControl.RenderTransform>
        <TranslateTransform x:Name="SlideTransform" X="0"/>
    </UserControl.RenderTransform>
    
    <UserControl.Triggers>
        <!-- Entrance Animation -->
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.3">
                        <DoubleAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut"/>
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                    <DoubleAnimation Storyboard.TargetName="SlideTransform" Storyboard.TargetProperty="X" 
                                   From="400" To="0" Duration="0:0:0.3">
                        <DoubleAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut"/>
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </UserControl.Triggers>
    
    <Border x:Name="ToastBorder" CornerRadius="16" BorderThickness="1.5" Background="#0DFFFFFF" Padding="16">
        <Border.Effect>
            <DropShadowEffect x:Name="ToastShadow" BlurRadius="20" ShadowDepth="0" Opacity="0.3" RenderingBias="Performance"/>
        </Border.Effect>
        
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <!-- Icon -->
            <Grid x:Name="IconContainer" Width="24" Height="24" Margin="0,0,12,0" VerticalAlignment="Top">
                <!-- Success Icon (Checkmark) -->
                <Path x:Name="SuccessIcon" Data="M9,20.42L2.79,14.21L5.62,11.38L9,14.77L18.88,4.88L21.71,7.71L9,20.42Z" 
                      Fill="#10B981" Stretch="Uniform" Visibility="Collapsed">
                    <Path.RenderTransform>
                        <ScaleTransform x:Name="SuccessScale" ScaleX="0" ScaleY="0" CenterX="12" CenterY="12"/>
                    </Path.RenderTransform>
                </Path>
                
                <!-- Error Icon (X) -->
                <Path x:Name="ErrorIcon" Data="M19,6.41L17.59,5L12,10.59L6.41,5L5,6.41L10.59,12L5,17.59L6.41,19L12,13.41L17.59,19L19,17.59L13.41,12L19,6.41Z" 
                      Fill="#EF4444" Stretch="Uniform" Visibility="Collapsed">
                    <Path.RenderTransform>
                        <TranslateTransform x:Name="ErrorShake" X="0"/>
                    </Path.RenderTransform>
                </Path>
                
                <!-- Warning Icon (Alert) -->
                <Path x:Name="WarningIcon" Data="M12,2L1,21H23L12,2M12,6L19.53,19H4.47L12,6M11,10V14H13V10H11M11,16V18H13V16H11Z" 
                      Fill="#F59E0B" Stretch="Uniform" Visibility="Collapsed" Opacity="1">
                    <Path.RenderTransform>
                        <ScaleTransform ScaleX="1" ScaleY="1" CenterX="12" CenterY="12"/>
                    </Path.RenderTransform>
                </Path>
            </Grid>
            
            <!-- Content -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock x:Name="TitleText" FontSize="14" FontWeight="SemiBold" Foreground="White" TextWrapping="Wrap"/>
                <TextBlock x:Name="MessageText" FontSize="12.5" Foreground="#CBD5E1" TextWrapping="Wrap" Margin="0,4,0,0"/>
            </StackPanel>

            <!-- Undo Button — visible only when HasUndo = true.
                 BoolToVisibilityConverter is from the global Converters.xaml dictionary. -->
            <Button Grid.Column="2"
                    Content="{Binding UndoLabel}"
                    Command="{Binding UndoCommand}"
                    Visibility="{Binding HasUndo,
                        Converter={StaticResource BoolToVisibilityConverter},
                        FallbackValue=Collapsed}"
                    Style="{DynamicResource Button.Secondary}"
                    Height="28"
                    Padding="12,0"
                    Margin="8,0,0,0"
                    VerticalAlignment="Center"
                    Cursor="Hand"/>
        </Grid>
    </Border>
</UserControl>
