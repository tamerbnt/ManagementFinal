<UserControl x:Class="Management.Presentation.Views.Shell.CommandPaletteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="{DynamicResource OverlayBackdrop}"> <!-- Dimmed Overlay -->
        <Border Width="640" VerticalAlignment="Top" Margin="0,100,0,0"
                Background="{DynamicResource SurfaceBg}" CornerRadius="24"
                BorderThickness="1" BorderBrush="{DynamicResource SeparatorStroke}">
            <Border.Effect>
                <StaticResource ResourceKey="Shadow.Modal" />
            </Border.Effect>
            <Grid Margin="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="64"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Search Input -->
                <Grid Margin="24,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Path Data="M9.5,3A6.5,6.5 0 0,1 16,9.5C16,11.11 15.41,12.59 14.44,13.73L19.71,19L18.29,20.41L13.01,15.14C11.87,16.11 10.39,16.7 8.78,16.7C4.81,16.7 1.6,13.49 1.6,9.52C1.6,5.55 4.81,2.34 8.78,2.34C10.39,2.34 11.87,2.93 13.01,3.9L9.5,3M9.5,5A4.5,4.5 0 0,0 5,9.5A4.5,4.5 0 0,0 9.5,14A4.5,4.5 0 0,0 14,9.5A4.5,4.5 0 0,0 9.5,5Z" 
                          Fill="{DynamicResource TextTertiaryBrush}" Stretch="Uniform" Width="20" VerticalAlignment="Center"/>
                    <TextBox x:Name="SearchBox" Grid.Column="1" 
                             Background="Transparent" BorderThickness="0"
                             Foreground="{DynamicResource TextPrimaryBrush}" FontSize="18"
                             VerticalContentAlignment="Center" Margin="16,0"
                             Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"/>
                    <Border Grid.Column="2" Background="{DynamicResource SeparatorStroke}" CornerRadius="4" Padding="6,2" VerticalAlignment="Center">
                        <TextBlock Text="{DynamicResource TerminologyEscLabel}" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="10" FontWeight="Bold"/>
                    </Border>
                </Grid>

                <Border Grid.Row="1" Height="1" Background="{DynamicResource SeparatorStroke}"/>

                <!-- Results -->
                <ItemsControl Grid.Row="2" ItemsSource="{Binding Results}" MaxHeight="400">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border x:Name="ItemBorder" Padding="24,12" Background="Transparent">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="{Binding Label}" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="14" FontWeight="Medium"/>
                                        <TextBlock Text="{Binding Description}" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12"/>
                                    </StackPanel>
                                    <TextBlock Grid.Column="1" Text="{Binding Category}" 
                                               Foreground="{DynamicResource TextTertiaryBrush}" FontSize="10" 
                                               VerticalAlignment="Center" FontWeight="Bold"/>
                                </Grid>
                            </Border>
                            <DataTemplate.Triggers>
                                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                    <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource FacilityAccentDimBrush}"/>
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </Border>
    </Grid>
</UserControl>
