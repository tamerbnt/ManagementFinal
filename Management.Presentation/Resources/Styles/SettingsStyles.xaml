<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Management.Presentation.Views.Settings">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="../Tokens/Colors.xaml" />
        <ResourceDictionary Source="../Tokens/DesignTokens.xaml" />
        <ResourceDictionary Source="../Typography.xaml" />
        <ResourceDictionary Source="../Iconography.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <!-- Sidebar Menu Style -->
    <Style x:Key="Settings.SidebarListBox" TargetType="ListBox">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="12" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
        <Setter Property="ItemContainerStyle">
            <Setter.Value>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="Padding" Value="16,12" />
                    <Setter Property="Margin" Value="0,2" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Secondary}" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border x:Name="border"
                                        Background="{TemplateBinding Background}"
                                        CornerRadius="8"
                                        SnapsToDevicePixels="True">
                                    <ContentPresenter Margin="{TemplateBinding Padding}" />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="{DynamicResource Brush.Surface.Subtle}" />
                                        <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Primary}" />
                                        <Setter Property="FontWeight" Value="SemiBold" />
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="{DynamicResource Brush.Surface.Subtle}" />
                                        <Setter TargetName="border" Property="Opacity" Value="0.6" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Settings Row Style -->
    <Style x:Key="Settings.Row" TargetType="Border">
        <Setter Property="Padding" Value="0,20" />
        <Setter Property="Background" Value="Transparent"/>
    </Style>

    <!-- Settings Button Style (Outline) -->
    <Style x:Key="Settings.Button" TargetType="Button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Primary}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Brush.Border.Subtle}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="16,8" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource Brush.Surface.Subtle}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#E5E7EB" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Global Settings Accordion Style -->
    <Style x:Key="GlobalSettingsAccordionStyle" TargetType="Expander">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Margin" Value="0,0,0,16"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Expander">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{DynamicResource SeparatorStroke}"
                            BorderThickness="0,0,0,1">
                        <StackPanel>
                            <ToggleButton x:Name="HeaderSite"
                                        Content="{TemplateBinding Header}"
                                        ContentTemplate="{TemplateBinding HeaderTemplate}"
                                        ContentTemplateSelector="{TemplateBinding HeaderTemplateSelector}"
                                        FontFamily="{TemplateBinding FontFamily}"
                                        FontSize="{TemplateBinding FontSize}"
                                        FontWeight="SemiBold"
                                        Foreground="{DynamicResource TextPrimaryBrush}"
                                        Padding="0,20"
                                        HorizontalContentAlignment="Stretch"
                                        IsChecked="{Binding IsExpanded, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Style>
                                    <Style TargetType="ToggleButton">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ToggleButton">
                                                    <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                                        <Grid>
                                                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                                            <Path x:Name="Arrow"
                                                                  Data="M 0 0 L 4 4 L 8 0"
                                                                  Stroke="{DynamicResource TextTertiaryBrush}"
                                                                  StrokeThickness="2"
                                                                  HorizontalAlignment="Right"
                                                                  VerticalAlignment="Center"
                                                                  RenderTransformOrigin="0.5,0.5">
                                                                <Path.RenderTransform>
                                                                    <RotateTransform Angle="0"/>
                                                                </Path.RenderTransform>
                                                            </Path>
                                                        </Grid>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsChecked" Value="True">
                                                            <Setter TargetName="Arrow" Property="RenderTransform">
                                                                <Setter.Value>
                                                                    <RotateTransform Angle="180"/>
                                                                </Setter.Value>
                                                            </Setter>
                                                        </Trigger>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="{DynamicResource SurfaceBgDim}"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ToggleButton.Style>
                            </ToggleButton>
                            <ContentPresenter x:Name="ExpandSite"
                                            Visibility="Collapsed"
                                            Margin="0,0,0,24"
                                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                            Focusable="false"/>
                        </StackPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsExpanded" Value="true">
                            <Setter TargetName="ExpandSite" Property="Visibility" Value="Visible"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Titan Grid Item Style (ListViewItem) -->
    <Style x:Key="TitanGridItem" TargetType="ListViewItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="BorderBrush" Value="{DynamicResource SeparatorStroke}"/>
        <Setter Property="Padding" Value="0,12"/>
        <Setter Property="Margin" Value="0"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListViewItem">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource SurfaceBgDim}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="{DynamicResource FacilityAccentDimBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Plan Row Template -->
    <DataTemplate x:Key="PlanRowTemplate">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>

            <!-- Status Dot -->
            <Ellipse Grid.Column="0" Width="8" Height="8" HorizontalAlignment="Center">
                <Ellipse.Style>
                    <Style TargetType="Ellipse">
                        <Setter Property="Fill" Value="{DynamicResource StatusSuccessBrush}"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsActive}" Value="False">
                                <Setter Property="Fill" Value="{DynamicResource TextTertiaryBrush}"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Ellipse.Style>
            </Ellipse>

            <!-- Plan Name -->
            <TextBlock Grid.Column="1" Text="{Binding Name}" Style="{StaticResource BodyMedium}" VerticalAlignment="Center"/>

            <!-- Duration -->
            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="{Binding DurationDays}" Style="{StaticResource Body}"/>
                <TextBlock Text=" " Style="{StaticResource Body}"/>
                <TextBlock Text="{DynamicResource Terminology.Settings.Editor.DurationDays.Label}" Style="{StaticResource Body}"/>
            </StackPanel>

            <!-- Price -->
            <TextBlock Grid.Column="3" 
                       Text="{Binding Price, StringFormat='{}{0:N2} DA'}" 
                       Style="{StaticResource BodyMedium}" 
                       HorizontalAlignment="Right" 
                       VerticalAlignment="Center"/>

            <!-- Actions -->
            <StackPanel Grid.Column="4" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Command="{Binding EditPlanCommand, RelativeSource={RelativeSource AncestorType={x:Type local:PlanSectionView}}}"
                        CommandParameter="{Binding}"
                        Style="{StaticResource IconButtonStyle}"
                        Margin="0,0,4,0">
                    <Path Data="{StaticResource Icon.Edit}" Style="{StaticResource Style.Icon.Micro}" Fill="{DynamicResource TextSecondaryBrush}"/>
                </Button>
                <Button Command="{Binding DeletePlanCommand, RelativeSource={RelativeSource AncestorType={x:Type local:PlanSectionView}}}"
                        CommandParameter="{Binding}"
                        Style="{StaticResource IconButtonStyle}">
                    <Path Data="{StaticResource Icon.Delete}" Style="{StaticResource Style.Icon.Micro}" Fill="{DynamicResource StatusErrorBrush}"/>
                </Button>
            </StackPanel>
        </Grid>
    </DataTemplate>

    <!-- Service Row Template -->
    <DataTemplate x:Key="ServiceRowTemplate">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>

            <!-- Status Dot -->
            <Ellipse Grid.Column="0" Width="8" Height="8" HorizontalAlignment="Center">
                <Ellipse.Style>
                    <Style TargetType="Ellipse">
                        <Setter Property="Fill" Value="{DynamicResource StatusSuccessBrush}"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsActive}" Value="False">
                                <Setter Property="Fill" Value="{DynamicResource TextTertiaryBrush}"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Ellipse.Style>
            </Ellipse>

            <!-- Service Name -->
            <TextBlock Grid.Column="1" Text="{Binding Name}" Style="{StaticResource BodyMedium}" VerticalAlignment="Center"/>

            <!-- Duration -->
            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="{Binding DurationMinutes}" Style="{StaticResource Body}"/>
                <TextBlock Text=" Min" Style="{StaticResource Body}"/>
            </StackPanel>

            <!-- Price -->
            <TextBlock Grid.Column="3" 
                       Text="{Binding Price, StringFormat='{}{0:N2} DA'}" 
                       Style="{StaticResource BodyMedium}" 
                       HorizontalAlignment="Right" 
                       VerticalAlignment="Center"/>

            <!-- Actions -->
            <StackPanel Grid.Column="4" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Command="{Binding EditServiceCommand, RelativeSource={RelativeSource AncestorType={x:Type local:ServiceSectionView}}}"
                        CommandParameter="{Binding}"
                        Style="{StaticResource IconButtonStyle}"
                        Margin="0,0,4,0">
                    <Path Data="{StaticResource Icon.Edit}" Style="{StaticResource Style.Icon.Micro}" Fill="{DynamicResource TextSecondaryBrush}"/>
                </Button>
                <Button Command="{Binding DeleteServiceCommand, RelativeSource={RelativeSource AncestorType={x:Type local:ServiceSectionView}}}"
                        CommandParameter="{Binding}"
                        Style="{StaticResource IconButtonStyle}">
                    <Path Data="{StaticResource Icon.Delete}" Style="{StaticResource Style.Icon.Micro}" Fill="{DynamicResource StatusErrorBrush}"/>
                </Button>
            </StackPanel>
        </Grid>
    </DataTemplate>

</ResourceDictionary>

