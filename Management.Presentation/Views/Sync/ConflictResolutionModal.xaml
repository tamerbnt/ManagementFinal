<UserControl x:Class="Management.Presentation.Views.Sync.ConflictResolutionModal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="clr-namespace:Management.Presentation.Resources.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="700">

    <Border Background="{DynamicResource CardBackgroundBrush}" CornerRadius="16" Width="700" Height="500">
        <Border.Effect>
            <DropShadowEffect BlurRadius="40" ShadowDepth="16" Opacity="0.1" Color="{DynamicResource ShadowColor}"/>
        </Border.Effect>

        <Grid Margin="32">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,24">
                <TextBlock Text="{DynamicResource Strings.Global.SyncConflictDetected}" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                <TextBlock Text="{Binding EntityName}" FontSize="14" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,4,0,0"/>
                <TextBlock Text="{Binding ConflictDescription}" FontSize="13" Foreground="{DynamicResource TextTertiaryBrush}" Margin="0,2,0,0"/>
            </StackPanel>

            <!-- Comparison Area -->
            <Grid Grid.Row="1" Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="32"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Local Version -->
                <Border Grid.Column="0" Background="{DynamicResource SurfaceBg}" CornerRadius="12" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="20">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                            <Ellipse Width="8" Height="8" Fill="{DynamicResource StatusWarningBrush}" Margin="0,0,8,0"/>
                            <TextBlock Text="{DynamicResource Strings.Sync.YourDeviceLocal}" FontWeight="SemiBold" Foreground="{DynamicResource TextSecondaryBrush}"/>
                        </StackPanel>
                        <TextBlock Grid.Row="1" Text="{Binding LocalVersionDetails}" FontSize="13" Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap" LineHeight="20"/>
                    </Grid>
                </Border>

                <!-- Divider -->
                <Border Grid.Column="1" Background="Transparent">
                    <Grid>
                        <Path Data="M0,0 M1,1" Stroke="{DynamicResource SeparatorStroke}" StrokeThickness="1" StrokeDashArray="4 4" VerticalAlignment="Center" Stretch="Uniform"/> 
                        <TextBlock Text="{DynamicResource Strings.Sync.VS}" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" Foreground="{DynamicResource TextTertiaryBrush}" Background="{DynamicResource CardBackgroundBrush}" Padding="4"/>
                    </Grid>
                </Border>

                <!-- Server Version -->
                <Border Grid.Column="2" Background="{DynamicResource SurfaceBg}" CornerRadius="12" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="20">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                            <Ellipse Width="8" Height="8" Fill="{DynamicResource FacilityAccentBrush}" Margin="0,0,8,0"/>
                            <TextBlock Text="{DynamicResource Strings.Sync.ServerCloud}" FontWeight="SemiBold" Foreground="{DynamicResource TextSecondaryBrush}"/>
                        </StackPanel>
                        <TextBlock Grid.Row="1" Text="{Binding ServerVersionDetails}" FontSize="13" Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap" LineHeight="20"/>
                    </Grid>
                </Border>
            </Grid>

            <!-- Actions -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Command="{Binding KeepLocalCommand}" Content="{DynamicResource Strings.Sync.KeepMyChanges}" 
                        Height="46" Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Foreground="{DynamicResource TextPrimaryBrush}"
                        Style="{StaticResource GhostButton}"/> <!-- Assuming global ghost button or default -->
                
                <Button Grid.Column="2" Command="{Binding KeepServerCommand}" Content="{DynamicResource Strings.Sync.OverwritewithServer}"
                        Height="46" Background="{DynamicResource FacilityAccentBrush}" BorderThickness="0" Foreground="White">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="10">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource FacilityAccentBrush}"/>
                                    <Setter Property="Opacity" Value="0.8"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Grid>
    </Border>
</UserControl>
