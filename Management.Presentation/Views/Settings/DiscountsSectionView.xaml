<UserControl x:Class="Management.Presentation.Views.Settings.DiscountsSectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Management.Presentation.Views.Settings"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="700">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,24">
            <StackPanel>
                <TextBlock Text="{DynamicResource Terminology.Settings.Discounts.Title}" Style="{DynamicResource Title2}" Foreground="{DynamicResource TextPrimaryBrush}"/>
                <TextBlock Text="{DynamicResource Terminology.Settings.Discounts.Subtitle}" Style="{DynamicResource Caption}" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,4,0,0"/>
            </StackPanel>
            
            <Button Content="{DynamicResource Terminology.Settings.Discounts.Action.Add}" 
                    HorizontalAlignment="Right" 
                    Style="{DynamicResource Button.Primary}"
                    Command="{Binding CreateDiscountCommand}"
                    Width="160" Height="40"/>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Titles -->
                <RowDefinition Height="*"/>    <!-- List -->
            </Grid.RowDefinitions>

            <!-- Grid Headers -->
            <Grid Grid.Row="0" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="40"/>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="100"/>
                </Grid.ColumnDefinitions>
                <!-- 0: Dot -->
                <TextBlock Grid.Column="1" Text="{DynamicResource Terminology.Settings.Discounts.Field.Name}" Style="{StaticResource Caption}" FontWeight="Bold"/>
                <TextBlock Grid.Column="2" Text="{DynamicResource Terminology.Settings.Discounts.Field.Type}" Style="{StaticResource Caption}" FontWeight="Bold"/>
                <TextBlock Grid.Column="3" Text="{DynamicResource Terminology.Settings.Discounts.Field.Value}" Style="{StaticResource Caption}" FontWeight="Bold" HorizontalAlignment="Right"/>
                <TextBlock Grid.Column="4" Text="" Style="{StaticResource Caption}"/>
            </Grid>

            <!-- Virtualized List -->
            <ListView Grid.Row="1" 
                      ItemsSource="{Binding Discounts}"
                      ItemContainerStyle="{StaticResource TitanGridItem}"
                      ItemTemplate="{StaticResource DiscountRowTemplate}"
                      BorderThickness="0"
                      Background="Transparent"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      ScrollViewer.VerticalScrollBarVisibility="Auto"
                      VirtualizingPanel.VirtualizationMode="Recycling"
                      ScrollViewer.CanContentScroll="True">
            </ListView>
        </Grid>
    </Grid>
</UserControl>
