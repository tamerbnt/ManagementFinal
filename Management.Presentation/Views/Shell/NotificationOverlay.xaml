<UserControl x:Class="Management.Presentation.Views.Shell.NotificationOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <Storyboard x:Key="SlideDown">
            <DoubleAnimation Storyboard.TargetName="Transformation"
                             Storyboard.TargetProperty="Y"
                             From="-100" To="0" Duration="0:0:0.4">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="NotificationBorder"
                             Storyboard.TargetProperty="Opacity"
                             From="0" To="1" Duration="0:0:0.2"/>
        </Storyboard>
    </UserControl.Resources>
    
    <Grid IsHitTestVisible="True" Background="Transparent">
        <Border x:Name="NotificationBorder" Width="600" Height="80" 
                Background="{DynamicResource SurfaceBg}" CornerRadius="24"
                BorderThickness="1" BorderBrush="{DynamicResource SeparatorStroke}"
                HorizontalAlignment="Center" VerticalAlignment="Top"
                Opacity="0">
            <Border.Style>
                <Style TargetType="Border">
                     <Style.Triggers>
                        <!-- Hit-Test Guard: Pass through clicks when invisible/faded -->
                        <Trigger Property="Opacity" Value="0">
                            <Setter Property="IsHitTestVisible" Value="False"/>
                        </Trigger>
                     </Style.Triggers>
                </Style>
            </Border.Style>
            <Border.RenderTransform>
                <TranslateTransform x:Name="Transformation" Y="-100"/>
            </Border.RenderTransform>
            
            <Grid Margin="24,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <Ellipse Width="12" Height="12" Fill="{DynamicResource AccentBrush}" VerticalAlignment="Center"/>
                
                <TextBlock Grid.Column="1" Text="{Binding CurrentMessage}" 
                           Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center" 
                           Margin="16,0" FontSize="14"/>
                
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="{DynamicResource TerminologyUndoLabel}" Style="{DynamicResource PrimaryButton}" 
                            Command="{Binding UndoCommand}"
                            Visibility="{Binding HasUndo, Converter={StaticResource BoolToVis}}"
                            Height="32" Padding="12,0" Margin="0,0,8,0"/>
                    <Button Style="{DynamicResource IconButtonStyle}" 
                            Command="{Binding DismissCommand}"
                            Foreground="{DynamicResource TextTertiaryBrush}">
                         <Path Data="M18 6 6 18M6 6l12 12" Stroke="{DynamicResource TextTertiaryBrush}" StrokeThickness="2" StrokeLineJoin="Round" StrokeStartLineCap="Round" StrokeEndLineCap="Round"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
