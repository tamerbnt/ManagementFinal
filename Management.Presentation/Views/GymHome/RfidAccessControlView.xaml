<UserControl x:Class="Management.Presentation.Views.GymHome.RfidAccessControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:local="clr-namespace:Management.Presentation.Views.GymHome"
             mc:Ignorable="d" 
             Width="520" Height="400">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>
    
    <!-- Using a soft shadow and rounded corners as per Titan Light Theme -->
    <Border Background="{DynamicResource Brush.Surface.Primary}" 
            CornerRadius="16" 
            Padding="0" 
            Effect="{StaticResource Shadow3}"
            BorderThickness="1"
            BorderBrush="{DynamicResource Brush.Border.Primary}">
        
        <Grid>
            <!-- TOP ACCENT BAR -->
            <Border Height="6" VerticalAlignment="Top" CornerRadius="16,16,0,0">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Background" Value="{DynamicResource Brush.Accent.Primary}"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding AccessGranted}" Value="True">
                                <Setter Property="Background" Value="{DynamicResource Brush.Status.Success}"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding AccessDenied}" Value="True">
                                <Setter Property="Background" Value="{DynamicResource Brush.Status.Error}"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding AccessWarning}" Value="True">
                                <Setter Property="Background" Value="{DynamicResource Brush.Status.Warning}"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
            </Border>
            
            <Grid Margin="32">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- Title / Status -->
                    <RowDefinition Height="*"/>    <!-- Content (Avatar & Name) -->
                    <RowDefinition Height="Auto"/> <!-- Description / Reason -->
                    <RowDefinition Height="Auto"/> <!-- Actions / Timer -->
                </Grid.RowDefinitions>
                
                <!-- STATUS HEADER -->
                <TextBlock Grid.Row="0"
                           FontFamily="{StaticResource Font.Primary}" 
                           FontSize="28" 
                           FontWeight="Black" 
                           HorizontalAlignment="Center" 
                           Margin="0,0,0,24">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <!-- Default Fallback -->
                            <Setter Property="Text" Value="SYSTEM MSG"/>
                            <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Primary}"/>
                            
                            <!-- Granted -->
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding AccessGranted}" Value="True">
                                    <Setter Property="Text" Value="ACCESS GRANTED"/>
                                    <Setter Property="Foreground" Value="{DynamicResource Brush.Status.Success}"/>
                                </DataTrigger>
                                
                                <!-- Denied -->
                                <DataTrigger Binding="{Binding AccessDenied}" Value="True">
                                    <Setter Property="Text" Value="ACCESS DENIED"/>
                                    <Setter Property="Foreground" Value="{DynamicResource Brush.Status.Error}"/>
                                </DataTrigger>
                                
                                <!-- Warning -->
                                <DataTrigger Binding="{Binding AccessWarning}" Value="True">
                                    <Setter Property="Text" Value="WARNING"/>
                                    <Setter Property="Foreground" Value="{DynamicResource Brush.Status.Warning}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <!-- MAIN CONTENT (AVATAR & NAME) -->
                <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <!-- AVATAR -->
                    <Border Width="100" Height="100" CornerRadius="50" Background="{DynamicResource Brush.Surface.Secondary}" HorizontalAlignment="Center" Margin="0,0,0,16">
                        <TextBlock Text="{Binding AvatarInitials}" 
                                   VerticalAlignment="Center" 
                                   HorizontalAlignment="Center" 
                                   FontSize="40" 
                                   FontWeight="Bold" 
                                   Foreground="{DynamicResource Brush.Text.Secondary}"/>
                    </Border>
                    
                    <!-- MEMBER NAME -->
                    <TextBlock Text="{Binding Result.Member.FullName, FallbackValue='Unknown Member'}" 
                               FontSize="22" 
                               FontWeight="Bold" 
                               Foreground="{DynamicResource Brush.Text.Primary}" 
                               HorizontalAlignment="Center" 
                               TextAlignment="Center" 
                               TextWrapping="Wrap"/>
                </StackPanel>

                <!-- REASON / DETAILS -->
                <Border Grid.Row="2" 
                        Background="{DynamicResource Brush.Surface.Secondary}" 
                        CornerRadius="8" 
                        Padding="16" 
                        Margin="0,24,0,24">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Result.Message}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Result.Message}" Value="">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel>
                        <!-- NEW REASON HEADER -->
                        <TextBlock Text="Reason for Denial:" 
                                   FontSize="16" 
                                   FontWeight="Bold" 
                                   Foreground="{DynamicResource Brush.Status.Error}" 
                                   HorizontalAlignment="Center" 
                                   Margin="0,0,0,8">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding AccessDenied}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        
                        <!-- ACTUAL MESSAGE -->
                        <TextBlock Text="{Binding Result.Message}" 
                                   FontSize="15" 
                                   FontWeight="SemiBold" 
                                   HorizontalAlignment="Center" 
                                   TextAlignment="Center" 
                                   TextWrapping="Wrap">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Secondary}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding AccessDenied}" Value="True">
                                            <Setter Property="Foreground" Value="{DynamicResource Brush.Status.Error}"/>
                                            <Setter Property="FontSize" Value="18"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding AccessWarning}" Value="True">
                                            <Setter Property="Foreground" Value="{DynamicResource Brush.Status.Warning}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </Border>

                <!-- LOWER ACTIONS / TIMER -->
                <Grid Grid.Row="3">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- DYNAMIC BUTTONS -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Left">
                        
                        <Button Content="Renew Membership" 
                                Command="{Binding RenewMembershipCommand}" 
                                Visibility="{Binding ShowRenewButton, Converter={StaticResource BoolToVis}}" 
                                Style="{StaticResource Button.Primary}" 
                                Margin="0,0,12,0" 
                                Width="160" Height="40"/>
                                
                        <Button Content="Create Profile" 
                                Command="{Binding CreateMemberCommand}" 
                                Visibility="{Binding ShowCreateButton, Converter={StaticResource BoolToVis}}" 
                                Style="{StaticResource Button.Primary}" 
                                Margin="0,0,12,0" 
                                Width="160" Height="40"/>

                    </StackPanel>
                    
                    <!-- CLOSE BUTTON / AUTO-CLOSE TIMER -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                        
                        <TextBlock VerticalAlignment="Center" 
                                   Foreground="{DynamicResource Brush.Text.Tertiary}" 
                                   Margin="0,0,12,0"
                                   Visibility="{Binding IsAutoClosing, Converter={StaticResource BoolToVis}}"
                                   FontSize="13">
                            <Run Text="Closing in "/>
                            <Run Text="{Binding RemainingTime}"/>
                            <Run Text="s"/>
                        </TextBlock>

                        <Button Content="Dismiss" 
                                Command="{Binding CloseCommand}" 
                                Style="{StaticResource Button.Secondary}" 
                                Width="100" Height="40"/>
                    </StackPanel>
                </Grid>
                
            </Grid>
        </Grid>
    </Border>
</UserControl>
