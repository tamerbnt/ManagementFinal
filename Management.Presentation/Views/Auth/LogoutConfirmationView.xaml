<UserControl x:Class="Management.Presentation.Views.Auth.LogoutConfirmationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:behaviors="clr-namespace:Management.Presentation.Behaviors"
             xmlns:local="clr-namespace:Management.Presentation.Views.Auth"
             xmlns:controls="clr-namespace:Management.Presentation.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="400">
    <Border Style="{DynamicResource GlobalModalContainerStyle}"
            Width="480"
            Padding="{DynamicResource PaddingModal}"
            VerticalAlignment="Center">

        <i:Interaction.Behaviors>
            <behaviors:ModalEntranceBehavior IsOpen="True"/>
        </i:Interaction.Behaviors>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="{DynamicResource MarginHeaderBottom}">
                <TextBlock Text="Log out" 
                           Style="{DynamicResource Title2}"
                           Foreground="{DynamicResource TextPrimaryBrush}"/>
            </StackPanel>

            <StackPanel Grid.Row="1" Margin="{DynamicResource MarginSectionBottom}">
                <TextBlock Text="Are you sure you want to log out?" 
                           Style="{DynamicResource Body}"
                           Foreground="{DynamicResource TextSecondaryBrush}"
                           TextWrapping="Wrap"/>
            </StackPanel>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" 
                        Content="Cancel" 
                        Command="{Binding CancelCommand}"
                        Style="{DynamicResource SecondaryButton}"/>

                <Button Grid.Column="2" 
                        Content="Log out" 
                        Command="{Binding ConfirmCommand}"
                        Style="{DynamicResource PrimaryButton}"
                        Background="{DynamicResource StatusErrorBrush}"
                        BorderBrush="{DynamicResource StatusErrorBrush}"
                        Foreground="White"/>
            </Grid>
        </Grid>
    </Border>
</UserControl>
