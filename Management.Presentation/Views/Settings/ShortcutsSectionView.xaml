<UserControl x:Class="Management.Presentation.Views.Settings.ShortcutsSectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Management.Presentation.Views.Settings"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <CollectionViewSource x:Key="GroupedShortcuts" Source="{Binding Shortcuts}">
            <CollectionViewSource.GroupDescriptions>
                <PropertyGroupDescription PropertyName="Category"/>
            </CollectionViewSource.GroupDescriptions>
        </CollectionViewSource>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="*"/>    <!-- List -->
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="{DynamicResource Terminology.Settings.Shortcuts.Title}" 
                       Style="{StaticResource Title2}"/>
            <TextBlock Text="{DynamicResource Terminology.Settings.Shortcuts.Subtitle}" 
                       Style="{StaticResource BodyMedium}" 
                       Foreground="{DynamicResource TextSecondaryBrush}"/>
        </StackPanel>

        <ListView Grid.Row="1" 
                  x:Name="ShortcutsList"
                  ItemsSource="{Binding Source={StaticResource GroupedShortcuts}}"
                  BorderThickness="0"
                  Background="Transparent"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                  ScrollViewer.VerticalScrollBarVisibility="Auto">
            <ListView.GroupStyle>
                <GroupStyle>
                    <GroupStyle.HeaderTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}" 
                                       Style="{StaticResource Title3}" 
                                       Foreground="{DynamicResource FacilityAccentBrush}"
                                       Margin="0,20,0,12"
                                       FontWeight="Bold"/>
                        </DataTemplate>
                    </GroupStyle.HeaderTemplate>
                </GroupStyle>
            </ListView.GroupStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Background="{DynamicResource ComponentBg}" 
                            CornerRadius="12" 
                            Padding="16,12" 
                            Margin="0,0,0,8"
                            BorderBrush="{DynamicResource SeparatorStroke}"
                            BorderThickness="1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="3*"/>
                            </Grid.ColumnDefinitions>
                            
                            <Border Grid.Column="0" 
                                    Background="{DynamicResource FacilityAccentBrush}" 
                                    Opacity="0.1" 
                                    CornerRadius="6" 
                                    HorizontalAlignment="Left"
                                    Padding="8,4">
                                <TextBlock Text="{Binding Keys}" 
                                           Foreground="{DynamicResource FacilityAccentBrush}" 
                                           FontWeight="SemiBold"
                                           FontFamily="Consolas"
                                           FontSize="13"/>
                            </Border>
                            <TextBlock Grid.Column="0" 
                                       Text="{Binding Keys}" 
                                       Foreground="{DynamicResource FacilityAccentBrush}" 
                                       FontWeight="SemiBold"
                                       FontFamily="Consolas"
                                       FontSize="13"
                                       HorizontalAlignment="Left"
                                       Padding="8,4"
                                       Panel.ZIndex="1"/>

                            <TextBlock Grid.Column="1" 
                                       Text="{Binding Description}" 
                                       Foreground="{DynamicResource TextPrimaryBrush}" 
                                       VerticalAlignment="Center"
                                       FontSize="14"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <ContentPresenter />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>
    </Grid>
</UserControl>
