<UserControl x:Class="Management.Presentation.Components.Layout.SideSheetHost"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="400"
             Visibility="Collapsed">
    <Grid>
        <!-- Backdrop -->
        <Border Background="#4D0F172A" MouseDown="Backdrop_MouseDown"/>

        <!-- Side Sheet Container -->
        <Border x:Name="SheetContainer"
                HorizontalAlignment="Right"
                Background="{DynamicResource Brush.Window.Background}"
                Width="450"
                BorderBrush="{DynamicResource Brush.Border.Subtle}"
                BorderThickness="1,0,0,0">
            <Border.RenderTransform>
                <TranslateTransform x:Name="SheetTransform" X="450"/>
            </Border.RenderTransform>

            <Grid Margin="24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header Actions -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,0,0,24">
                    <Button Width="40" Height="40"
                            Style="{StaticResource Button.Ghost}"
                            Click="Close_Click"
                            Content="â†"
                            FontSize="20"/>
                    <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}"
                               VerticalAlignment="Center"
                               Margin="12,0,0,0"
                               FontSize="{DynamicResource Text.Size.Title2}"
                               FontFamily="{DynamicResource Font.Primary}"
                               FontWeight="Bold"
                               Foreground="{DynamicResource Brush.Text.Primary}"/>
                </StackPanel>

                <ContentPresenter Grid.Row="1" Content="{Binding Content, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
