<UserControl x:Class="Management.Presentation.Views.GymHome.RegisterWalkInModal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:vm="clr-namespace:Management.Presentation.ViewModels.GymHome"
        xmlns:premium="clr-namespace:Management.Presentation.Controls.Premium"
        mc:Ignorable="d" 
        d:DataContext="{d:DesignInstance Type=vm:RegisterWalkInViewModel}"
        Background="Transparent">

    <premium:ModalShellControl Title="{Binding Title}"
                               Subtitle="{DynamicResource Terminology.GymHome.WalkIn.Subtitle}"
                               Width="480" MaxWidth="480"
                               CancelCommand="{Binding CancelCommand}"
                               SaveCommand="{Binding SaveLeadCommand}"
                               PrimaryActionText="{DynamicResource Terminology.Global.Save}">
        
        <Grid Margin="0,32">
            <!-- Form -->
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="400">
                <!-- Name -->
                <StackPanel Margin="0,0,0,16">
                    <TextBlock Text="{DynamicResource Terminology.Gym.Registration.Name}" Style="{DynamicResource FormLabel}" Margin="0,0,0,8"/>
                    <TextBox Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged}" 
                             Style="{DynamicResource Input.Apple}"
                             Tag="{DynamicResource Terminology.GymHome.WalkIn.NamePlaceholder}"/>
                </StackPanel>
 
                <!-- Phone -->
                <StackPanel Margin="0,0,0,24">
                    <TextBlock Text="{DynamicResource Terminology.Gym.Registration.Phone}" Style="{DynamicResource FormLabel}" Margin="0,0,0,8"/>
                    <TextBox Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged}" 
                             Style="{DynamicResource Input.Apple}"
                             Tag="+213..."/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </premium:ModalShellControl>
</UserControl>
