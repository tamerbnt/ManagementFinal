<UserControl x:Class="Management.Presentation.Views.Auth.DiscoverySkeletonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="400">
    
    <UserControl.Resources>
        <Storyboard x:Key="PulseAnimation" RepeatBehavior="Forever">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="0.4" To="0.8" Duration="0:0:0.8"
                             AutoReverse="True"/>
        </Storyboard>
        
        <Style TargetType="Rectangle">
            <Setter Property="Fill" Value="#F3F4F6"/>
            <Setter Property="RadiusX" Value="6"/>
            <Setter Property="RadiusY" Value="6"/>
        </Style>
    </UserControl.Resources>

    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard Storyboard="{StaticResource PulseAnimation}"/>
        </EventTrigger>
    </UserControl.Triggers>

    <Grid VerticalAlignment="Top" Margin="40,16,40,24">
        <StackPanel>
            <!-- Header Logo Placeholder -->
            <Grid Margin="0,8,0,8">
                <Rectangle Width="100" Height="80" HorizontalAlignment="Left" Margin="-10,0,0,0"/>
            </Grid>

            <!-- Title & Subtitle Placeholders -->
            <Rectangle Width="180" Height="30" HorizontalAlignment="Left" Margin="0,0,0,8" RadiusX="8" RadiusY="8"/>
            <Rectangle Width="160" Height="16" HorizontalAlignment="Left" Margin="0,0,0,24"/>

            <!-- Facility Card Placeholders -->
            <StackPanel>
                <ItemsControl ItemsSource="{Binding Items}">
                    <ItemsControl.Template>
                        <ControlTemplate>
                            <StackPanel IsItemsHost="True"/>
                        </ControlTemplate>
                    </ItemsControl.Template>
                    <!-- Static placeholders for the skeleton -->
                    <Border Background="#F8FAFC" CornerRadius="12" Padding="12" Margin="0,0,0,10" BorderThickness="1.5" BorderBrush="Transparent">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Rectangle Grid.Column="0" Width="36" Height="36" RadiusX="8" RadiusY="8"/>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="12,0">
                                <Rectangle Width="100" Height="14" HorizontalAlignment="Left" Margin="0,0,0,6"/>
                                <Rectangle Width="140" Height="10" HorizontalAlignment="Left"/>
                            </StackPanel>
                            <Border Grid.Column="2" Width="20" Height="20" CornerRadius="10" Background="#F3F4F6" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>

                    <Border Background="#F8FAFC" CornerRadius="12" Padding="12" Margin="0,0,0,10" BorderThickness="1.5" BorderBrush="Transparent">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Rectangle Grid.Column="0" Width="36" Height="36" RadiusX="8" RadiusY="8"/>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="12,0">
                                <Rectangle Width="120" Height="14" HorizontalAlignment="Left" Margin="0,0,0,6"/>
                                <Rectangle Width="130" Height="10" HorizontalAlignment="Left"/>
                            </StackPanel>
                            <Border Grid.Column="2" Width="20" Height="20" CornerRadius="10" Background="#F3F4F6" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>

                    <Border Background="#F8FAFC" CornerRadius="12" Padding="12" Margin="0,0,0,10" BorderThickness="1.5" BorderBrush="Transparent">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Rectangle Grid.Column="0" Width="36" Height="36" RadiusX="8" RadiusY="8"/>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="12,0">
                                <Rectangle Width="110" Height="14" HorizontalAlignment="Left" Margin="0,0,0,6"/>
                                <Rectangle Width="150" Height="10" HorizontalAlignment="Left"/>
                            </StackPanel>
                            <Border Grid.Column="2" Width="20" Height="20" CornerRadius="10" Background="#F3F4F6" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                </ItemsControl>
            </StackPanel>

            <!-- Bottom Buttons Placeholder -->
            <Grid Margin="0,16,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Margin="0,0,16,0">
                    <Rectangle Height="52" RadiusX="16" RadiusY="16" Margin="0,0,0,8"/>
                    <Rectangle Height="40" RadiusX="12" RadiusY="12"/>
                </StackPanel>
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <Rectangle Width="60" Height="10" Margin="0,0,0,4" HorizontalAlignment="Center"/>
                    <Rectangle Width="110" Height="80"/>
                </StackPanel>
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>
