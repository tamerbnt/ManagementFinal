<Window x:Class="Management.Presentation.Views.Auth.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Management.Presentation.Views.Auth"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        mc:Ignorable="d"
        Title="{DynamicResource Strings.Auth.Login}" Height="768" Width="1024"
        WindowStyle="None"
        AllowsTransparency="False"
        Background="{DynamicResource Brush.Window.Background}"
        WindowStartupLocation="CenterScreen">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0"
                          GlassFrameThickness="-1"
                          CornerRadius="24"
                          ResizeBorderThickness="0"/>
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />

        <!-- CUSTOM PASSWORD STYLE (Based on Input.Field) -->
        <Style x:Key="Input.Password" TargetType="PasswordBox">
            <Setter Property="FontFamily" Value="Segoe UI Variable Display, Segoe UI"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Padding" Value="0,8"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Brush.Border.Subtle}"/>
            <Setter Property="BorderThickness" Value="0,0,0,2"/>
            <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Primary}"/>
            <Setter Property="CaretBrush" Value="{DynamicResource Brush.Accent.Primary}"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="PasswordChar" Value="Ã¢â€”Â"/>
            
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Grid>
                            <Border x:Name="BaseBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"/>
                            
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          VerticalAlignment="Center"
                                          Margin="{TemplateBinding Padding}"/>

                            <Border x:Name="FocusOverlay"
                                    Background="{DynamicResource Brush.Accent.Primary}" 
                                    Height="2"
                                    VerticalAlignment="Bottom"
                                    HorizontalAlignment="Stretch"
                                    RenderTransformOrigin="0.5, 0.5">
                                <Border.RenderTransform>
                                    <ScaleTransform ScaleX="0"/>
                                </Border.RenderTransform>
                            </Border>
                        </Grid>
                        
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="FocusOverlay"
                                                             Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                             To="1" Duration="0:0:0.3">
                                                <DoubleAnimation.EasingFunction>
                                                    <CubicEase EasingMode="EaseOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="FocusOverlay"
                                                             Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                             To="0" Duration="0:0:0.25">
                                                <DoubleAnimation.EasingFunction>
                                                    <CubicEase EasingMode="EaseIn"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid MouseLeftButtonDown="OnMouseLeftButtonDown">
        <!-- LAYER 0: BACKGROUND -->
        <Border x:Name="AppBackground" 
                Background="{DynamicResource Brush.Sidebar.Background}">
            <Border.Effect>
                <BlurEffect Radius="40"/>
            </Border.Effect>
        </Border>

        <!-- LAYER 1: THE CARD -->
        <Border x:Name="LoginCard" 
                Width="420" VerticalAlignment="Center" HorizontalAlignment="Center"
                Background="#FFFFFF"
                CornerRadius="24" Padding="40">
            <Border.Effect>
                <DropShadowEffect BlurRadius="50" ShadowDepth="15" Opacity="0.3" Color="#000000" Direction="270"/>
            </Border.Effect>

            <StackPanel>
                <TextBlock Text="{DynamicResource Strings.Auth.WelcomeBack}" 
                           FontSize="24" FontWeight="Bold" 
                           Foreground="{DynamicResource Brush.Text.Primary}" 
                           HorizontalAlignment="Center" Margin="0,0,0,8"/>
                
                <TextBlock Text="{Binding FacilityName}" 
                           FontSize="16" FontWeight="Medium"
                           Foreground="{DynamicResource Brush.Accent.Primary}" 
                           HorizontalAlignment="Center" Margin="0,0,0,40"/>

                <!-- EMAIL -->
                <TextBlock Text="{DynamicResource Strings.Auth.EMAILADDRESS}" FontSize="11" FontWeight="Bold" Foreground="#9CA3AF" Margin="0,0,0,4"/>
                <TextBox Style="{DynamicResource Input.Field}" 
                         Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" 
                         Margin="0,0,0,24"/>

                <!-- PASSWORD -->
                <TextBlock Text="{DynamicResource Strings.Auth.PASSWORD}" FontSize="11" FontWeight="Bold" Foreground="#9CA3AF" Margin="0,0,0,4"/>
                <PasswordBox Style="{StaticResource Input.Password}" 
                             x:Name="PasswordBox"
                             PasswordChanged="PasswordBox_PasswordChanged"
                             Margin="0,0,0,8"/>

                <!-- ERROR MESSAGE -->
                <TextBlock Text="{Binding ErrorMessage}" 
                           Foreground="#DC2626" 
                           FontSize="12" 
                           TextWrapping="Wrap"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,24"
                           Height="16"/>

                <!-- LOGIN BUTTON -->
                <Button Command="{Binding LoginCommand}" Height="48">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="{DynamicResource Brush.Accent.Primary}"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="12" RenderTransformOrigin="0.5,0.5">
                                            <Border.RenderTransform>
                                                <ScaleTransform x:Name="ButtonScale" ScaleX="1" ScaleY="1"/>
                                            </Border.RenderTransform>
                                            <Grid>
                                                <TextBlock x:Name="txt" Text="{DynamicResource Strings.Auth.SignIn}" 
                                                         FontSize="16" FontWeight="SemiBold" 
                                                         VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                                
                                                <ProgressBar x:Name="progress" 
                                                            IsIndeterminate="True" 
                                                            Width="60" Height="4" 
                                                            VerticalAlignment="Center" HorizontalAlignment="Center"
                                                            Background="Transparent" Foreground="White" BorderThickness="0"
                                                            Visibility="Collapsed"/>
                                            </Grid>
                                        </Border>
                                         <ControlTemplate.Triggers>
                                             <Trigger Property="IsPressed" Value="True">
                                                 <Trigger.EnterActions>
                                                     <BeginStoryboard>
                                                         <Storyboard>
                                                             <DoubleAnimation Storyboard.TargetName="ButtonScale" Storyboard.TargetProperty="ScaleX" To="0.98" Duration="0:0:0.05"/>
                                                             <DoubleAnimation Storyboard.TargetName="ButtonScale" Storyboard.TargetProperty="ScaleY" To="0.98" Duration="0:0:0.05"/>
                                                         </Storyboard>
                                                     </BeginStoryboard>
                                                 </Trigger.EnterActions>
                                                 <Trigger.ExitActions>
                                                     <BeginStoryboard>
                                                         <Storyboard>
                                                             <DoubleAnimation Storyboard.TargetName="ButtonScale" Storyboard.TargetProperty="ScaleX" To="1.0" Duration="0:0:0.1"/>
                                                             <DoubleAnimation Storyboard.TargetName="ButtonScale" Storyboard.TargetProperty="ScaleY" To="1.0" Duration="0:0:0.1"/>
                                                         </Storyboard>
                                                     </BeginStoryboard>
                                                 </Trigger.ExitActions>
                                             </Trigger>
                                            <DataTrigger Binding="{Binding IsBusy}" Value="True">
                                                <Setter TargetName="txt" Property="Visibility" Value="Collapsed"/>
                                                <Setter TargetName="progress" Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                                            </Trigger>
                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter TargetName="border" Property="Opacity" Value="0.6"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Border>

        <!-- LAYER 2: CLOSE BUTTON -->
        <Button x:Name="CloseButton" 
                Click="CloseButton_Click" 
                HorizontalAlignment="Right" VerticalAlignment="Top" 
                Margin="20" Background="Transparent" BorderThickness="0" Cursor="Hand">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" Padding="8">
                        <Path Data="M19,6.41L17.59,5L12,10.59L6.41,5L5,6.41L10.59,12L5,17.59L6.41,19L12,13.41L17.59,19L19,17.59L13.41,12L19,6.41z" 
                              Fill="White" Width="14" Height="14" Stretch="Uniform"/>
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>
    </Grid>
</Window>
