<UserControl x:Class="Management.Presentation.Views.GymHome.MultiSaleCartModal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:Management.Presentation.ViewModels.GymHome"
             xmlns:premium="clr-namespace:Management.Presentation.Controls.Premium"
             xmlns:dashboard="clr-namespace:Management.Presentation.Views.Shell"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:converters="clr-namespace:Management.Presentation.Converters"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:MultiSaleCartViewModel}"
             Background="Transparent">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:CountToBooleanConverter x:Key="CountToBooleanConverter"/>
        <converters:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter"/>
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
        <converters:NullToVisibilityConverter x:Key="InverseNullToVisibilityConverter" Inverted="True"/>
        <converters:IntToVisibilityConverter x:Key="GreaterThanZeroConverter"/>
        <converters:EnumToBooleanConverter x:Key="EnumToBoolConverter"/>
        <converters:EnumToVisibilityConverter x:Key="EnumToVisibilityConverter"/>
        <converters:EnumToBooleanConverter x:Key="EnumToBooleanConverter"/>

        <!-- Icon Aliases -->
        <StreamGeometry x:Key="IconMember">M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2M9 7a4 4 0 1 0 0-8 4 4 0 0 0 0 8M22 21v-2a4 4 0 0 0-3-3.87M16 3.13a4 4 0 0 1 0 7.75</StreamGeometry>
        <StreamGeometry x:Key="IconSearch">M11 19a8 8 0 1 0 0-16 8 8 0 0 0 0 16zM21 21l-4.35-4.35</StreamGeometry>
        <StreamGeometry x:Key="IconClose">M18 6L6 18M6 6l12 12</StreamGeometry>
        <StreamGeometry x:Key="IconPinFilled">M12 17v5M9 10.76a2 2 0 0 1-1.11 1.79l-1.78.9A.5.5 0 0 0 6 13.9V15a1 1 0 0 0 1 1h10a1 1 0 0 0 1-1v-1.1a.5.5 0 0 0-.11-.31l-1.78-.9A2 2 0 0 1 15 10.76V6a1 1 0 0 1 1-1h1a1 1 0 0 0 0-2H7a1 1 0 0 0 0 2h1a1 1 0 0 1 1 1z</StreamGeometry>
        
        <!-- Selection Highlight Style -->
        <Style x:Key="SelectionItemStyle" TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="ItemBorder" 
                                CornerRadius="12" 
                                BorderThickness="1.5" 
                                BorderBrush="{DynamicResource SeparatorStroke}"
                                Background="{DynamicResource Brush.Surface.Glass}"
                                Padding="12"
                                Margin="0,0,0,8"
                                Cursor="Hand">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="BorderBrush" Value="{DynamicResource FacilityAccentBrush}"/>
                                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource FacilityAccentDimBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemBorder" Property="BorderBrush" Value="{DynamicResource FacilityAccentBrush}"/>
                                <Setter TargetName="ItemBorder" Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Stepper Button Style -->
        <Style x:Key="StepperButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="{DynamicResource SurfaceBgDim}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="16">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource SurfaceAccentBg}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TabToggleStyle" TargetType="RadioButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border" Background="Transparent" CornerRadius="10" Padding="24,8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center">
                                <ContentPresenter.ContentTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}" FontWeight="SemiBold" FontSize="14" Foreground="{DynamicResource Brush.Text.Secondary}"/>
                                    </DataTemplate>
                                </ContentPresenter.ContentTemplate>
                            </ContentPresenter>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource SurfaceBg}"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <premium:ModalShellControl Title="{Binding Title}"
                               Subtitle="{DynamicResource Terminology.GymHome.MultiSale.Subtitle}"
                               Width="900" 
                               CancelCommand="{Binding CancelCommand}"
                               SaveCommand="{Binding CheckoutCommand}"
                               PrimaryActionText="{DynamicResource Terminology.GymHome.MultiSale.Summary.Checkout}">

        <Grid Height="500" Margin="0,12,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Tabs -->
                <RowDefinition Height="*"/>    <!-- Split Content -->
            </Grid.RowDefinitions>

            <!-- 1. TABS -->
            <Border Grid.Row="0" Background="{DynamicResource SurfaceBgDim}" CornerRadius="14" Padding="4" HorizontalAlignment="Center" Margin="0,0,0,24">
                <StackPanel Orientation="Horizontal">
                    <RadioButton Content="{DynamicResource Terminology.GymHome.MultiSale.Tab.Products}" 
                                 IsChecked="{Binding CurrentTab, Converter={StaticResource EnumToBoolConverter}, ConverterParameter=Products}"
                                 Command="{Binding SwitchTabCommand}"
                                 CommandParameter="Products"
                                 Style="{StaticResource TabToggleStyle}"/>
                    <RadioButton Content="{DynamicResource Terminology.GymHome.MultiSale.Tab.WalkIn}" 
                                 IsChecked="{Binding CurrentTab, Converter={StaticResource EnumToBoolConverter}, ConverterParameter=WalkIn}"
                                 Command="{Binding SwitchTabCommand}"
                                 CommandParameter="WalkIn"
                                 Style="{StaticResource TabToggleStyle}"/>
                </StackPanel>
            </Border>

            <!-- 2. SPLIT LAYOUT -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.7*"/> <!-- Left: Selection -->
                    <ColumnDefinition Width="32"/>    <!-- Spacer -->
                    <ColumnDefinition Width="*"/>    <!-- Right: Control Panel (Stationary) -->
                </Grid.ColumnDefinitions>

                <!-- LEFT COLUMN: INDEPENDENT SCROLLING LIST -->
                <Grid Grid.Column="0">
                    <!-- Products View -->
                    <StackPanel Visibility="{Binding CurrentTab, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=Products}">
                        <premium:PremiumSearchBox Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"
                                                 Placeholder="{DynamicResource Terminology.Modal.MultiSale.SearchPlaceholder}"
                                                 IconData="{StaticResource IconSearch}"
                                                 Margin="0,0,0,12"/>
                        
                        <ListBox ItemsSource="{Binding Products}" 
                                 SelectedItem="{Binding SelectedProduct}"
                                 ItemContainerStyle="{StaticResource SelectionItemStyle}"
                                 Style="{StaticResource PremiumListBox}"
                                 BorderThickness="0"
                                 Height="400"
                                 VirtualizingStackPanel.IsVirtualizing="True"
                                 VirtualizingStackPanel.VirtualizationMode="Recycling"> 
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel VerticalAlignment="Center">
                                            <StackPanel Orientation="Horizontal">
                                                <Path Data="{StaticResource IconPinFilled}" Fill="{DynamicResource FacilityAccentBrush}" 
                                                      Width="12" Height="12" Margin="0,0,8,0" VerticalAlignment="Center" Stretch="Uniform"
                                                      Visibility="{Binding IsPinned, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                                <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14" Foreground="{DynamicResource Brush.Text.Primary}"/>
                                            </StackPanel>
                                            <TextBlock Text="{Binding Category}" FontSize="11" Foreground="{DynamicResource Brush.Text.Tertiary}"/>
                                        </StackPanel>
                                        <TextBlock Grid.Column="1" 
                                                   Text="{Binding Price, StringFormat='{}{0:N2} DA'}" 
                                                   FontSize="14" 
                                                   FontWeight="Bold" 
                                                   Foreground="{DynamicResource Brush.Text.Primary}"
                                                   Margin="0,0,16,0" 
                                                   VerticalAlignment="Center"/>
                                        
                                        <!-- Row Unselect (VISIBLE ONLY WHEN SELECTED) -->
                                        <Button Grid.Column="2" 
                                                Command="{Binding DataContext.ClearSelectionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                Style="{DynamicResource Button.Ghost}" Width="32" Height="32" Padding="8"
                                                Visibility="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}, Converter={StaticResource BooleanToVisibilityConverter}}">
                                            <Path Data="M19,6.41L17.59,5L12,10.59L6.41,5L5,6.41L10.59,12L5,17.59L6.41,19L12,13.41L17.59,19L19,17.59L13.41,12L19,6.41Z" Fill="#EF4444" Stretch="Uniform"/>
                                        </Button>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>

                    <!-- Walk-In View -->
                    <StackPanel Visibility="{Binding CurrentTab, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=WalkIn}">
                        <premium:PremiumSearchBox Text="{Binding WalkInSearchQuery, UpdateSourceTrigger=PropertyChanged}"
                                                 Placeholder="{DynamicResource Terminology.Modal.MultiSale.SearchWalkIn}"
                                                 IconData="{StaticResource IconSearch}"
                                                 Margin="0,0,0,12"/>

                        <ListBox ItemsSource="{Binding FilteredWalkInPlans}" 
                                 SelectedItem="{Binding SelectedWalkInPlan}"
                                 ItemContainerStyle="{StaticResource SelectionItemStyle}"
                                 Style="{StaticResource PremiumListBox}"
                                 BorderThickness="0"
                                 Height="400"
                                 VirtualizingStackPanel.IsVirtualizing="True"
                                 VirtualizingStackPanel.VirtualizationMode="Recycling">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14" Foreground="{DynamicResource Brush.Text.Primary}"/>
                                            <TextBlock Text="{DynamicResource Terminology.Modal.MultiSale.StandardEntry}" FontSize="11" Foreground="{DynamicResource Brush.Text.Tertiary}"/>
                                        </StackPanel>
                                        <TextBlock Grid.Column="1" Text="{Binding Price, StringFormat='{}{0:N2} DA'}" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,16,0" Foreground="{DynamicResource Brush.Text.Primary}"/>

                                        <!-- Row Unselect -->
                                        <Button Grid.Column="2" 
                                                Command="{Binding DataContext.ClearSelectionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                Style="{DynamicResource Button.Ghost}" Width="32" Height="32" Padding="8"
                                                Visibility="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}, Converter={StaticResource BooleanToVisibilityConverter}}">
                                            <Path Data="M19,6.41L17.59,5L12,10.59L6.41,5L5,6.41L10.59,12L5,17.59L6.41,19L12,13.41L17.59,19L19,17.59L13.41,12L19,6.41Z" Fill="#EF4444" Stretch="Uniform"/>
                                        </Button>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>
                </Grid>

                <!-- RIGHT COLUMN: INDEPENDENT SCROLLING CONTROLS -->
                <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto" Height="400">
                    <StackPanel>
                        <!-- 0. MEMBER SEARCH -->
                        <Border Background="{DynamicResource Brush.Surface.Card}" CornerRadius="16" Padding="12" Margin="0,0,0,16" BorderBrush="{DynamicResource SeparatorStroke}" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource Terminology.Modal.MultiSale.LinkMember}" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource Brush.Text.Tertiary}" Margin="0,0,0,8"/>
                                
                                 <Grid Visibility="{Binding SelectedMember, Converter={StaticResource InverseNullToVisibilityConverter}}">
                                    <premium:PremiumSearchBox 
                                                            Text="{Binding MemberSearchQuery, UpdateSourceTrigger=PropertyChanged}"
                                                            Placeholder="{DynamicResource Terminology.Modal.MultiSale.SearchMembers}"
                                                            IconData="{StaticResource IconSearch}"/>
                                    
                                    <!-- Search Results Popup -->
                                    <Popup IsOpen="{Binding IsPopupOpen, Mode=OneWay}" 
                                           PlacementTarget="{Binding RelativeSource={RelativeSource AncestorType=Grid}}"
                                           StaysOpen="False" AllowsTransparency="True" Placement="Bottom" PopupAnimation="Fade">
                                        <Border Background="{DynamicResource SurfaceBg}" 
                                                CornerRadius="12" 
                                                BorderThickness="1" 
                                                BorderBrush="{DynamicResource SeparatorStroke}" 
                                                Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Grid}}" 
                                                MinWidth="260"
                                                MaxHeight="350">
                                            <Border.Effect>
                                                <DropShadowEffect BlurRadius="15" ShadowDepth="2" Opacity="0.1" Color="Black"/>
                                            </Border.Effect>
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="*"/>
                                                </Grid.RowDefinitions>

                                                <!-- Loading State -->
                                                <Grid Grid.Row="0" Margin="12" Visibility="{Binding IsMemberSearching, Converter={StaticResource BoolToVis}}">
                                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                        <premium:AtriumMarkAnimation Width="20" Height="20" AnimationMode="Scan" MarkForeground="{DynamicResource FacilityAccentBrush}"/>
                                                        <TextBlock Text="Searching members..." Margin="8,0,0,0" VerticalAlignment="Center" FontSize="12" Foreground="{DynamicResource Brush.Text.Tertiary}"/>
                                                    </StackPanel>
                                                </Grid>

                                                <ListBox Grid.Row="1" ItemsSource="{Binding SearchedMembers}" 
                                                         Style="{StaticResource ListBox.SearchPopup}"
                                                         ItemTemplate="{StaticResource MemberSearchResultTemplate}"/>

                                                <!-- No Results State -->
                                                <TextBlock Grid.Row="1" Text="No members found" 
                                                           HorizontalAlignment="Center" Margin="20" 
                                                           Foreground="{DynamicResource Brush.Text.Tertiary}">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                            <Style.Triggers>
                                                                <MultiDataTrigger>
                                                                    <MultiDataTrigger.Conditions>
                                                                        <Condition Binding="{Binding SearchedMembers.Count}" Value="0"/>
                                                                        <Condition Binding="{Binding IsMemberSearching}" Value="False"/>
                                                                        <Condition Binding="{Binding IsPopupOpen}" Value="True"/>
                                                                    </MultiDataTrigger.Conditions>
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                </MultiDataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </Grid>
                                        </Border>
                                    </Popup>
                                </Grid>

                                <!-- Selected Member Badge -->
                                <Border Background="{DynamicResource StatusInfoBackground}" CornerRadius="10" Padding="10,6" Visibility="{Binding SelectedMember, Converter={StaticResource NullToVisibilityConverter}}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0,0,8,0">
                                            <Path Data="{StaticResource IconMembers}" Stroke="{DynamicResource FacilityAccentBrush}" StrokeThickness="1.5" Width="14" Height="14" Margin="0,0,8,0" Stretch="Uniform"/>
                                            <TextBlock Text="{Binding SelectedMember.FullName}" FontWeight="Bold" Foreground="{DynamicResource FacilityAccentBrush}" FontSize="13" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                        </StackPanel>
                                        
                                        <Button Grid.Column="1" Command="{Binding ClearSelectedMemberCommand}" Style="{DynamicResource Button.Ghost}" Width="24" Height="24" Padding="6" VerticalAlignment="Center">
                                            <Path Data="{StaticResource IconClose}" Stroke="{DynamicResource FacilityAccentBrush}" StrokeThickness="2" Stretch="Uniform"/>
                                        </Button>
                                    </Grid>
                                </Border>
                            </StackPanel>
                        </Border>
                        <!-- 0.5. MANUAL DISCOUNT -->
                        <Border Background="{DynamicResource Brush.Surface.Card}" CornerRadius="16" Padding="12" Margin="0,0,0,16" BorderBrush="{DynamicResource SeparatorStroke}" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource TerminologyManualDiscountLabel}" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource Brush.Text.Tertiary}" Margin="0,0,0,8"/>
                                <ComboBox ItemsSource="{Binding AvailableDiscounts}"
                                          SelectedItem="{Binding SelectedDiscount}"
                                          Style="{DynamicResource Input.ComboBox}"
                                          Height="42">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Name}"/>
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>
                            </StackPanel>
                        </Border>

                        <!-- 1. ACTIVE SELECTION DETAILS -->
                        <Border Background="{DynamicResource Brush.Surface.Card}" CornerRadius="16" Padding="20" Margin="0,0,0,16" BorderBrush="{DynamicResource SeparatorStroke}" BorderThickness="1">
                            <Grid MinHeight="140">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                
                                <!-- EMPTY STATE -->
                                <TextBlock Text="{DynamicResource Terminology.Modal.MultiSale.SelectItem}" 
                                         FontSize="12" Foreground="{DynamicResource Brush.Text.Tertiary}"
                                         HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center"
                                         Visibility="{Binding IsControlPanelEmpty, Converter={StaticResource BooleanToVisibilityConverter}}"
                                         Margin="0,20"/>

                                <!-- PRODUCT SELECTION CONTENT -->
                                <StackPanel Visibility="{Binding IsProductSelectionActive, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <Grid Margin="0,0,0,8">
                                        <TextBlock Text="{DynamicResource Terminology.Modal.MultiSale.MiniCart}" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource Brush.Text.Tertiary}"/>
                                        <Button Command="{Binding ClearSelectionCommand}" 
                                                Style="{DynamicResource Button.Ghost}"
                                                HorizontalAlignment="Right" Width="24" Height="24" Padding="6">
                                            <Path Data="{StaticResource IconClose}" Fill="#EF4444" Stretch="Uniform"/>
                                        </Button>
                                    </Grid>
                                    
                                    <TextBlock Text="{Binding SelectedProduct.Name}" FontSize="15" FontWeight="Bold" Foreground="{DynamicResource Brush.Text.Primary}" TextWrapping="Wrap"/>
                                    
                                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                        <TextBlock Text="{Binding SelectedProductPricing.OriginalPrice.Amount, StringFormat='{}{0:N2} DA'}" 
                                                   FontSize="12" Foreground="{DynamicResource Brush.Text.Tertiary}" TextDecorations="Strikethrough"
                                                   Visibility="{Binding SelectedProductPricing.IsDiscountApplied, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}"
                                                   Margin="0,0,8,0" VerticalAlignment="Bottom"/>
                                        <TextBlock Text="{Binding SelectedProductPricing.EffectivePrice.Amount, StringFormat='{}{0:N2} DA'}" 
                                                   FontSize="18" Foreground="{DynamicResource FacilityAccentBrush}" FontWeight="ExtraBold" VerticalAlignment="Bottom"/>
                                        
                                        <Border Background="{DynamicResource StatusSuccessBackground}" CornerRadius="10" Padding="6,2" Margin="8,0,0,0" VerticalAlignment="Bottom"
                                                Visibility="{Binding SelectedProductPricing.IsDiscountApplied, Converter={StaticResource BooleanToVisibilityConverter}}">
                                            <TextBlock Text="{Binding SelectedProductPricing.AppliedPromotionName, FallbackValue=PROMO}" FontSize="9" FontWeight="Bold" Foreground="{DynamicResource StatusSuccessBrush}"/>
                                        </Border>
                                    </StackPanel>
                                    
                                    <Grid Margin="0,16,0,0" HorizontalAlignment="Center">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="40"/>
                                            <ColumnDefinition Width="64"/>
                                            <ColumnDefinition Width="40"/>
                                        </Grid.ColumnDefinitions>
                                        <Button Grid.Column="0" Command="{Binding DecrementSelectedProductCommand}" Style="{StaticResource StepperButtonStyle}">
                                            <Path Data="M19,13H5V11H19V13Z" Fill="#4B5563" Stretch="Uniform" Margin="10"/>
                                        </Button>
                                        <TextBlock Grid.Column="1" Text="{Binding SelectedProductQuantity}" TextAlignment="Center" VerticalAlignment="Center" FontSize="22" FontWeight="ExtraBold" Foreground="{DynamicResource FacilityAccentBrush}"/>
                                        <Button Grid.Column="2" Command="{Binding IncrementSelectedProductCommand}" Style="{StaticResource StepperButtonStyle}">
                                            <Path Data="M19,13H13V19H11V13H5V11H11V5H13V11H19V13Z" Fill="#4B5563" Stretch="Uniform" Margin="10"/>
                                        </Button>
                                    </Grid>
                                </StackPanel>

                                <!-- WALK-IN SELECTION CONTENT -->
                                <StackPanel Visibility="{Binding IsWalkInSelectionActive, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <Grid Margin="0,0,0,8">
                                        <TextBlock Text="{DynamicResource Terminology.Modal.MultiSale.MiniCart}" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource Brush.Text.Tertiary}"/>
                                        <Button Command="{Binding ClearSelectionCommand}" 
                                                Style="{DynamicResource Button.Ghost}"
                                                HorizontalAlignment="Right" Width="24" Height="24" Padding="6">
                                            <Path Data="{StaticResource IconClose}" Fill="#EF4444" Stretch="Uniform"/>
                                        </Button>
                                    </Grid>
                                    <TextBlock Text="{Binding SelectedWalkInPlan.Name}" FontSize="15" FontWeight="Bold" Foreground="{DynamicResource Brush.Text.Primary}" TextWrapping="Wrap"/>
                                    <Grid Margin="0,16,0,0" HorizontalAlignment="Center">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="40"/>
                                            <ColumnDefinition Width="64"/>
                                            <ColumnDefinition Width="40"/>
                                        </Grid.ColumnDefinitions>
                                        <Button Grid.Column="0" Command="{Binding DecrementWalkInCommand}" Style="{StaticResource StepperButtonStyle}">
                                            <Path Data="M19,13H5V11H19V13Z" Fill="#4B5563" Stretch="Uniform" Margin="10"/>
                                        </Button>
                                        <TextBlock Grid.Column="1" Text="{Binding WalkInCount}" TextAlignment="Center" VerticalAlignment="Center" FontSize="22" FontWeight="ExtraBold" Foreground="{DynamicResource FacilityAccentBrush}"/>
                                        <Button Grid.Column="2" Command="{Binding IncrementWalkInCommand}" Style="{StaticResource StepperButtonStyle}">
                                            <Path Data="M19,13H13V19H11V13H5V11H11V5H13V11H19V13Z" Fill="#4B5563" Stretch="Uniform" Margin="10"/>
                                        </Button>
                                    </Grid>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- 2. MINI CART -->
                        <Border Background="{DynamicResource Brush.Surface.Glass}" CornerRadius="16" Padding="20" Margin="0,0,0,16">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource Terminology.Modal.MultiSale.MiniCart}" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource Brush.Text.Tertiary}" Margin="0,0,0,12"/>
                                <ListBox ItemsSource="{Binding CartItems}" 
                                         Style="{StaticResource PremiumListBox}"
                                         MaxHeight="300">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,0,0,10">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0">
                                                    <TextBlock Text="{Binding Name}" FontSize="13" FontWeight="SemiBold" MaxWidth="110" TextTrimming="CharacterEllipsis" Foreground="{DynamicResource Brush.Text.Primary}"/>
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="{Binding Price, StringFormat='{}{0:N2} DA'}" FontSize="11" Foreground="{DynamicResource Brush.Text.Tertiary}"/>
                                                        <!-- Promo Dot -->
                                                        <Ellipse Width="6" Height="6" Fill="#10B981" Margin="6,0,0,0" VerticalAlignment="Center" 
                                                                 Visibility="{Binding IsDiscounted, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                            <Ellipse.ToolTip>
                                                                <ToolTip Content="Promotional Discount Applied"/>
                                                            </Ellipse.ToolTip>
                                                        </Ellipse>
                                                    </StackPanel>
                                                </StackPanel>
                                                <TextBlock Grid.Column="1" Text="{Binding Quantity, StringFormat='x{0}'}" FontSize="13" FontWeight="Bold" Margin="12,0" VerticalAlignment="Center" Foreground="{DynamicResource Brush.Text.Primary}"/>
                                                <Button Grid.Column="2" 
                                                        Command="{Binding DataContext.RemoveProductFromCartCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}"
                                                        Style="{DynamicResource Button.Ghost}" Width="24" Height="24" Padding="6">
                                                    <Path Data="{StaticResource IconClose}" Fill="#EF4444" Stretch="Uniform"/>
                                                </Button>
                                            </Grid>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>

                                <!-- Guests Entry Item -->
                                <Grid Margin="0,4,0,0" Visibility="{Binding WalkInCount, Converter={StaticResource GreaterThanZeroConverter}}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="{Binding SelectedWalkInPlan.Name}" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource Brush.Text.Primary}"/>
                                        <TextBlock Text="{DynamicResource Terminology.Modal.MultiSale.StandardEntry}" FontSize="11" Foreground="{DynamicResource Brush.Text.Tertiary}"/>
                                    </StackPanel>
                                    <TextBlock Grid.Column="1" Text="{Binding WalkInCount, StringFormat='x{0}'}" FontSize="13" FontWeight="Bold" VerticalAlignment="Center" Foreground="{DynamicResource Brush.Text.Primary}"/>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <!-- 3. SUMMARY -->
                        <Border Background="{DynamicResource Brush.Surface.Card}" CornerRadius="16" Padding="20" BorderBrush="{DynamicResource SeparatorStroke}" BorderThickness="1">
                            <StackPanel>
                                <Grid Margin="0,0,0,6">
                                    <TextBlock Text="{DynamicResource Terminology.Modal.MultiSale.ProductsTotal}" FontSize="11" Foreground="{DynamicResource Brush.Text.Secondary}"/>
                                    <TextBlock Text="{Binding ProductsTotal, StringFormat='{}{0:N2} DA'}" FontSize="12" HorizontalAlignment="Right" FontWeight="SemiBold" Foreground="{DynamicResource Brush.Text.Primary}"/>
                                </Grid>
                                <Grid Margin="0,0,0,12">
                                    <TextBlock Text="{DynamicResource Terminology.Modal.MultiSale.EntriesTotal}" FontSize="11" Foreground="{DynamicResource Brush.Text.Secondary}"/>
                                    <TextBlock Text="{Binding WalkInTotal, StringFormat='{}{0:N2} DA'}" FontSize="12" HorizontalAlignment="Right" FontWeight="SemiBold" Foreground="{DynamicResource Brush.Text.Primary}"/>
                                </Grid>
                                <Separator Background="{DynamicResource SeparatorStroke}" Margin="0,0,0,12"/>
                                <StackPanel>
                                    <TextBlock Text="{DynamicResource Terminology.Global.Total}" FontSize="14" FontWeight="Bold" Margin="0,0,0,4" Foreground="{DynamicResource Brush.Text.Primary}"/>
                                    <TextBlock Text="{Binding GrandTotal, StringFormat='{}{0:N2} DA'}" FontSize="20" FontWeight="ExtraBold" Foreground="{DynamicResource FacilityAccentBrush}" HorizontalAlignment="Right"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Grid>
    </premium:ModalShellControl>
</UserControl>

