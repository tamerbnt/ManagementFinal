<UserControl x:Class="Management.Presentation.Components.Dashboard.RevenueByPlanCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="400">
    <Border Background="{DynamicResource CardBackgroundBrush}" CornerRadius="16">
        <Border.Effect>
            <StaticResource ResourceKey="Shadow.Level2" />
        </Border.Effect>
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header & Filters -->
            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Interactive Header Toggle -->
                <Button Command="{Binding CycleRevenueModeCommand}" Cursor="Hand" HorizontalAlignment="Left" Background="Transparent" BorderThickness="0" Padding="0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Width="24" Height="24" CornerRadius="8" Background="{DynamicResource FacilityAccentDimBrush}" Margin="0,0,12,0" VerticalAlignment="Center">
                            <Path Data="{StaticResource IconFinance}" Fill="{DynamicResource FacilityAccentBrush}" Stroke="{DynamicResource FacilityAccentBrush}" StrokeThickness="1.5" Width="12" Height="12" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Text="{Binding RevenueBreakdownTitle}" FontWeight="Bold" FontSize="14" Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                        <Path Data="M7,10L12,15L17,10H7Z" Fill="{DynamicResource TextSecondaryBrush}" Width="10" Height="8" Stretch="Uniform" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- Filters -->
                <Border Grid.Column="1" Background="{DynamicResource FacilitySidebarBrush}" CornerRadius="12" Padding="4">
                    <StackPanel Orientation="Horizontal">
                        <Button Content="{DynamicResource Terminology.Dashboard.Range.Today}" Command="{Binding SetPlanFilterCommand}" CommandParameter="Today" Margin="2,0" Padding="8,2">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource Button.Ghost}">
                                    <Setter Property="FontSize" Value="10"/>
                                    <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SelectedPlanFilter}" Value="Today">
                                            <Setter Property="Background" Value="{DynamicResource FacilityAccentBrush}"/>
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button Content="{DynamicResource Terminology.Dashboard.Range.ThisWeek}" Command="{Binding SetPlanFilterCommand}" CommandParameter="This Week" Margin="2,0" Padding="8,2">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource Button.Ghost}">
                                    <Setter Property="FontSize" Value="10"/>
                                    <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SelectedPlanFilter}" Value="This Week">
                                            <Setter Property="Background" Value="{DynamicResource FacilityAccentBrush}"/>
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button Content="{DynamicResource Terminology.Dashboard.Range.ThisMonth}" Command="{Binding SetPlanFilterCommand}" CommandParameter="This Month" Margin="2,0" Padding="8,2">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource Button.Ghost}">
                                    <Setter Property="FontSize" Value="10"/>
                                    <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SelectedPlanFilter}" Value="This Month">
                                            <Setter Property="Background" Value="{DynamicResource FacilityAccentBrush}"/>
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Plan Revenue List -->
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" MaxHeight="220">
                <ItemsControl ItemsSource="{Binding PlanRevenue}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="{Binding PlanName}" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                    <TextBlock FontSize="10" Foreground="{DynamicResource TextTertiaryBrush}">
                                        <Run Text="{Binding Count}"/><Run Text=" "/><Run Text="{DynamicResource Terminology.Dashboard.Stat.Sales}"/>
                                    </TextBlock>
                                </StackPanel>
                                <StackPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Revenue, StringFormat=C0, ConverterCulture=fr-DZ}" FontSize="12" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" HorizontalAlignment="Right"/>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>
