<Window x:Class="Management.Presentation.Views.Salon.AddStaffView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:behaviors="clr-namespace:Management.Presentation.Behaviors"
        mc:Ignorable="d" 
        Title="{DynamicResource Strings.Salon.AddStylistColumn}" Height="500" Width="450"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterOwner" SizeToContent="Height">

    <Border Style="{DynamicResource GlobalModalContainerStyle}"
            Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource MultiplyValueConverter}, ConverterParameter=0.45}"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">

        <i:Interaction.Behaviors>
            <behaviors:ModalEntranceBehavior IsOpen="True"/>
        </i:Interaction.Behaviors>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Header -->
                <RowDefinition Height="*"/>    <!-- List -->
                <RowDefinition Height="Auto"/> <!-- Footer -->
            </Grid.RowDefinitions>

            <!-- 1. Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,24">
                <Border Width="64" Height="64" 
                        Background="{DynamicResource Brush.Brand.Primary.Fade}" 
                        CornerRadius="32" 
                        HorizontalAlignment="Center" 
                        Margin="0,0,0,16">
                    <Path Data="{StaticResource IconUsers}" 
                          Style="{StaticResource Style.Icon.Large}" 
                          Stroke="{DynamicResource Brush.Brand.Primary}" 
                          HorizontalAlignment="Center" 
                          VerticalAlignment="Center"/>
                </Border>
                <TextBlock Text="{DynamicResource Terminology.Salon.Staff.Add.Title}" 
                           Style="{StaticResource Text.Title2}" 
                           Foreground="{DynamicResource Brush.Text.Primary}"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="{DynamicResource Terminology.Salon.Staff.Add.Select}" 
                           Style="{StaticResource Text.Caption}" 
                           Foreground="{DynamicResource Brush.Text.Secondary}"
                           HorizontalAlignment="Center"
                           Margin="0,4,0,0"/>
            </StackPanel>

            <!-- 2. Staff List -->
            <ListBox Grid.Row="1" 
                     ItemsSource="{Binding AvailableStaff}" 
                     SelectedItem="{Binding SelectedStaff}"
                     Height="220"
                     Margin="0,0,0,24"
                     Style="{StaticResource PremiumListBox}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="12" CornerRadius="12" Background="Transparent">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- Avatar -->
                                <Border Grid.Column="0" Width="40" Height="40" 
                                        CornerRadius="20" Background="{DynamicResource Brush.Surface.Secondary}"
                                        Margin="0,0,12,0">
                                    <TextBlock Text="{Binding FullName}" 
                                               FontSize="14" FontWeight="SemiBold"
                                               Foreground="{DynamicResource Brush.Text.Primary}"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"
                                               Opacity="0.8"/>
                                </Border>

                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding FullName}" 
                                               Style="{StaticResource Text.Body.Bold}" 
                                               Foreground="{DynamicResource Brush.Text.Primary}"/>
                                    <TextBlock Text="{Binding Role}" 
                                               Style="{StaticResource Text.Caption}" 
                                               Foreground="{DynamicResource Brush.Text.Secondary}"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!-- 3. Footer Actions -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="1.5*"/>
                </Grid.ColumnDefinitions>
                
                <Button Grid.Column="0" 
                        Content="{DynamicResource Terminology.Global.Cancel}" 
                        Command="{Binding CancelCommand}"
                        Style="{StaticResource SecondaryButton}"
                        Margin="0,0,12,0"
                        Height="48"/>

                <Button Grid.Column="1" 
                        Content="{DynamicResource Terminology.Salon.Staff.Add.Action}" 
                        Command="{Binding AddCommand}"
                        Style="{StaticResource PrimaryButton}"
                        IsEnabled="{Binding CanAddColumn}"
                        Height="48"/>
            </Grid>

            <!-- Loading Overlay -->
            <Grid Grid.RowSpan="3" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}" Panel.ZIndex="1000">
                <Border Background="{DynamicResource Brush.Surface.Glass}" CornerRadius="16"/>
                <ProgressBar IsIndeterminate="True" 
                             Foreground="{DynamicResource FacilityAccentBrush}"
                             Background="Transparent"
                             BorderThickness="0"
                             VerticalAlignment="Center" 
                             Width="150" 
                             Height="4"
                             HorizontalAlignment="Center"/>
            </Grid>
        </Grid>
    </Border>
</Window>
